# we can also configure our properties file this way

spring:
 application:
  name: superhero-api
 datasource:
  url: jdbc:mysql://localhost:3306/superheroes
  username: root
  password: root
  driver-class-name: com.mysql.cj.jdbc.Driver
 jpa:
  hibernate:
   ddl-auto: none

server:
 port: 8080