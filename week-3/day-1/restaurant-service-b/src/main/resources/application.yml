# remember to match this with the LB call in the gateway!!
spring:
 application:
  name: restaurant-service

server:
 port: 8083

# we do NOT need to say this should register-with-eureka, it does so by default
eureka:
 client:
  service-url:
   defaultZone: http://localhost:8761/eureka